<section id="services">
	{{#each services}}
		<div class="container" id="{{id}}">
			<div class="row">
				<div class="col-12 text-center">
					<h2 class="section-heading">{{title}}</h2>
				</div>
				<div class="col-12">
					<p>{{text}}</p>
				</div>
			</div>
		</div>
	{{/each}}
</section>
